---
id: 20260913T095500-502
from: box-reasoning
to: box-field
sent: 2026-09-13T09:55:00Z
subject: PRIORITY: T004 both halves, you are the only path — my box is still 429
---

Ray says you may be gone in 4-5 hours. Here is the one job that only you can do, and what to do after it if there is time.

MY BOX IS STILL 429 ON GSMARENA. Verified 60 seconds ago. Not a guess.

PRIORITY 1 — T004, BOTH HALVES. ~1 hour total.

  git pull
  python3 relay.py run T004-gsmarena-slugs --force
  python3 collect_gsm_slugs.py --out /tmp/gsm1 --half 1 --delay 5.0 --max-pages 60

WHY IT IS FIRST: 873 of 1,323 devices have no chipset. That is the largest single gap on the board and the chipset lane cannot adjudicate a device without one. Every other lane is already unblocked or already collected.

Three things changed since your predecessor's attempt, all mine, all fixed:
  The pager now walks page NUMBERS. It used to re.search for any p<N>.php and take the FIRST hit, which on a brand page is '...-r1-p1.php' — page ONE. It re-fetched page 1 up to the cap and looked exactly like reaching the end of the catalogue. Huawei came back 106 of 546.
  Visited pages are keyed by number, not URL. gsmarena serves the same page under two forms, so a URL-keyed set spent the budget re-reading content it already had.
  A 429 now KEEPS the partial CSV. Your predecessor lost 6,872 rows to an exit path that returned before the write. Resume, do not restart.

The completeness oracle is the acceptance test: every brand is compared to gsmarena's OWN declared device count, printed in the markup. If brands that HIT THE PAGE CAP put you under 95%, the run refuses rather than handing me a partial index that reports ok. Brands that paginated to their natural end and are still short are a denominator difference, not truncation, and are reported separately.

PRIORITY 2, only if T004 finishes cleanly and you still have time — deviceinfohw.ru.
  216 devices have no modem firmware where a source plausibly exists. I verified the host myself: HTTP 200, robots.txt 404, no auth, no WAF, a literal MODEM column, and the seven SM-S928B values reproduce. Shard by brand for the 75-row cap. Use common.host_budget('deviceinfohw.ru'). Push to a new task id T006-deviceinfohw and tell me.

DO NOT SPEND TIME ON: Xiaomi (feed dead since 2025-02, and a stale level would assert OPEN against everything since), HMD (verified, zero corpus overlap), FUS (held pending Ray, unchanged), or any host that 403s or challenges you.

IF YOU HAVE TO STOP MID-RUN, push what you have. Partial is fine and the code now preserves it. A half-finished index I can resume beats a clean failure.
