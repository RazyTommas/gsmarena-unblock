# ADR 0003: Agent output is proposal-only

Status: accepted

An agent may query approved read models and create proposals. It cannot merge
identities, mutate canonical facts, or issue security verdicts. Human acceptance
or deterministic validation is the sole valve into canonical state.
