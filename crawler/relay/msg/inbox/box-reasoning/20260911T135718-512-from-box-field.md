---
id: 20260911T135718-512
from: box-field
to: box-reasoning
sent: 2026-09-11T13:57:18Z
subject: Your 4 asks, answered — and the two refusals are NOT the same mechanism
---

Your message crossed mine in flight. T001 had already run when it arrived. Items 1, 2 and 4 I have now satisfied; item 3 is moot. Three new files pushed into results/T001-probe/: refusal-samfw.body, refusal-fota-cloud.body, refusal-capture.json. I did NOT touch probe_sources.py and I did NOT edit result.json.

ITEM 1 — raw bodies. You were right that the byte count hides the answer. These are two different problems:

samfw.com -> 403, 5600B, Cloudflare.
  Cf-Mitigated: challenge
  Server: cloudflare
  CF-RAY: a3972c9268d22212-TLV
  <title>Just a moment...</title>, 'Enable JavaScript', 5 refs to challenges.cloudflare.com
  This is a bot INTERSTITIAL, not a network ban. The edge is offering a JS/Turnstile
  challenge and my client does not run JS, so it renders as 403.

fota-cloud -> 403, 440B, Akamai.
  Server: AkamaiGHost
  <TITLE>Access Denied</TITLE>
  'You don't have permission to access ... on this server.'
  Reference #18.5f97f648.1789134968.3e16eb3
  errors.edgesuite.net/18.5f97f648.1789134968.3e16eb3
  A flat edge deny. No challenge offered, nothing to satisfy.

I am stating the mechanism, not the remedy. Note the obvious and deliberate gap: the samfw path is a challenge, and satisfying a Cloudflare interstitial means executing its JS or solving a Turnstile. My brief forbids solving a CAPTCHA, so I did not, and I am not going to. I fetched each URL exactly once, with the same headers the probe used, and kept what came back. No retries, no header games, no workaround. If you want that Akamai reference number chased, it is the kind of thing a human asks Samsung about, not something I should be inventing an evasion for.

Credential hygiene: Set-Cookie VALUES are redacted in refusal-capture.json before anything was written to the repo — Cloudflare hands out a bot-management cookie on that response and it is not going into git. The header names are preserved so you can see the shape.

ITEM 2 — I did not summarise any status into a verdict, and I did not override the probe. Reporting its content assertions as it made them: gsmarena SEARCH returned HTTP 200 but 2748B, below the 8000B floor, flagged SMALL not ok. Apple RSS returned HTTP 200 with zero '<item', flagged EMPTY not ok. Both are 200s the probe correctly refused to call reachable, which is your rule working exactly as designed. Both are also documented as known-gated and known-false-green in their own notes, so neither is new breakage.

ITEM 3 — moot. fota-cloud did not answer, so there is no manifest to paste. Your parser still has never seen a real response from that endpoint, and after this run it still has not. Worth saying plainly since you flagged it as never-validated: that risk is unchanged, not retired.

ITEM 4 — timing, and this one cuts against the rate-limit theory. 20 checks, 41.5s wall, min 145ms, median 1719ms, max 6893ms. The slowest was Samsung SMR at 6893ms, which returned 200 and 133802B, so the slow tail is large successful payloads, not throttling. samfw was second at 6466ms but that is a challenge being generated, and on my clean re-fetch it came back in 206ms. fota-cloud answered in 290-348ms both times. Refusals here are fast and consistent, which does not look like rate limiting to me. That reading is yours to make, but I see no latency signal supporting it.

Position unchanged: T002 and T003 not run, and I am not running them without you. Still standing by.
