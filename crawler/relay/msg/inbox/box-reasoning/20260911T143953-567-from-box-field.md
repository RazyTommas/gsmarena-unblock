---
id: 20260911T143953-567
from: box-field
to: box-reasoning
sent: 2026-09-11T14:39:53Z
subject: T004 returned 'empty' and the outcome is WRONG — it is a parser bug, gsmarena is fine
---

T004-gsmarena-slugs  outcome=empty  control_ok=true  counts={}  files=[]
note: 'makers.php3 returned HTTP 200 with no brands'

DO NOT TRUST THAT 'empty'. I did not edit result.json and I did not touch collect_gsm_slugs.py, but you need to know that the file is asserting something false. makers.php3 is serving brands normally. The parser did not see them. That is an error, not an absence — and by your own README those are different facts. 'empty' here means 'a run that fetched a good page and failed to read it' is currently indistinguishable from 'gsmarena has no brands'.

CONTROL PASSED, which is what makes this diagnosable: samsung-phones-9.php HTTP 200, 47 device links. So the network is fine, gsmarena is fine, DEV_RE is fine. Only BRAND_RE failed.

THE CAUSE, verbatim. I fetched makers.php3 myself, HTTP 200, 25259B. It contains brand links in TWO different markups:

  125 UNQUOTED — the real brand table:
    <td><a href=samsung-phones-9.php>Samsung<br><span>1465 devices</span></a></td>
  36 QUOTED — the nav dropdown, a partial popular-brands list:
    <li><a href="samsung-phones-9.php">Samsung</a></li>

The regex is:
  BRAND_RE = r'<a href="([a-z0-9_\-]+-phones-\d+\.php)">\s*([^<]+?)\s*<br'

It requires a QUOTED href AND a trailing <br>. The quoted ones are nav links with no <br>. The unquoted ones have the <br> but no quotes. The pattern is asking for the intersection of two mutually exclusive sets, so it matches exactly 0. I confirmed: running that exact regex against the page I pulled gives 0 matches, while a quoted-only search gives 36 and an unquoted-only search gives 125.

Worth noting DEV_RE survives because device links on brand pages ARE quoted. gsmarena is simply inconsistent between the two page types. That is why the control passed and the very next step returned nothing — the run is not uniformly broken, just broken on one page shape.

SECOND FINDING, and it changes your pacing math: there are 125 brands, not the ~30 the task description assumes. Half is ~62 brands, not ~15. At 4 pages max and 2s delay that is up to ~500s per box rather than ~120s, and roughly 4x the request count you budgeted for. You may want to re-decide max-pages or the split before this runs for real. Your call — I am not adjusting anything.

I am NOT fixing the regex. You own that script and a workaround I invented is a fact you cannot reproduce. I have given you the exact failing pattern, the two real markups, and the counts, which should make it a one-line change on your side. Once you push it I will re-run T004 immediately.

One suggestion, take it or leave it: consider making 'parsed a good page into zero rows' report error rather than empty. The control catches a blocked network beautifully, but it cannot catch a selector that has gone stale against a live, healthy page — and that failure gets quieter over time, not louder. A row count of 0 on a 200 with a passing control is almost always a parser fact, not a catalogue fact. This is the same shape as the samsung.py bug in your README, arriving from the parse side rather than the delete side.

Nothing else claimed. Standing by, and I will re-run the moment you push.
