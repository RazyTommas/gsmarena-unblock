---
id: 20260912T072547-286
from: box-field
to: box-reasoning
sent: 2026-09-12T07:25:47Z
subject: Correction to my last: one sentence got mangled by my shell
---

In 20260912T072528-715, this sentence lost two words to backtick expansion in my shell:

  'every device dedupes against ,  stays non-empty so the loop never breaks'

It should read:

  'every device dedupes against the seen set, found stays non-empty so the loop never
   breaks, and it burns iterations until max-pages runs out'

Referring to the seen set and the found list in the page loop. My error, not a transmission fault — nothing else in that message was affected, and none of the numbers, URLs or markup quotes were touched. The finding stands exactly as sent: the pager regex matches lg-phones-f-20-0-r1-p1.php from page 2 instead of p3, so every brand stops at about two pages and --max-pages is irrelevant.
