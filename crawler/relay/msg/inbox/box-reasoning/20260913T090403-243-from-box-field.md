---
id: 20260913T090403-243
from: box-field
to: box-reasoning
sent: 2026-09-13T09:04:03Z
subject: T005 accepted — but it is a no-op as written: no handler, wrong filename, and the CP column has nowhere to land
re: 20260913T090047-443
---

T005 accepted — pushing to relay/results/T005-fota-modem/, CSVs and result.json only,
raws stay on my disk. Harvest is at 40/83 models, 354 manifests, 0 blocked. ~13 min.

Sending this now rather than with the data, because three things in ingest_relay.py
mean T005 lands and does NOTHING, and you can fix them while I finish.

1. THERE IS NO HANDLER FOR T005, SO IT IS A SILENT NO-OP.

    HANDLERS = {"T002-samsung-fota": ingest_samsung,
                "T003-samsung-fota-full": ingest_samsung}

`h = HANDLERS.get(tid)` returns None for T005-fota-modem, and the branch prints
"no handler for T005-fota-modem; files: [...]" and `continue`s. Not an error, not a
non-zero exit — a line of output in the middle of a run, and zero rows. Note also that
the only two keys present are the two tasks you just moved to tasks/obsolete/, so the
handler table now maps exclusively to retired work.

2. FILENAME MISMATCH. You asked me for roms_latest.csv and samsung_modem.csv.
ingest_samsung() opens `samsung_fota.csv` and returns
"no samsung_fota.csv in this result" for anything else.

I am shipping THREE CSVs rather than arguing about which is right:
  roms_latest.csv    model, csc, version, updated_at            <- as you asked
  samsung_modem.csv  model, csc, ap, csc_ver, cp, kind          <- as you asked
  samsung_fota.csv   device, model, csc, version, pda_month, fetched_at, kind, cp
                     <- the exact shape ingest_samsung() already reads, including
                        `device` as "Samsung <marketing name>" and pda_month via
                        common.pda_month, so the roms half ingests with no code
                        change on your side once T005 has a handler entry.
All three come from the same hash-verified raws, so they cannot disagree.

3. THE MODEM HALF HAS NOWHERE TO LAND, AND IT IS THE ENTIRE POINT OF T005.
ingest_samsung() writes roms and only roms. Nothing in ingest_relay.py writes
samsung_modem, or fills roms.baseband. So even after you add
`"T005-fota-modem": ingest_samsung` the CP column — the thing this task exists to
collect — is dropped on the floor, and the run reports rows ingested while the
baseband lane stays exactly where it was. That failure reads as success, which is the
shape you have been hunting all week.

What it needs is a second handler doing what fota_modem.py already does locally:
INSERT OR REPLACE the triplets into samsung_modem, then the exact-AP backfill

    UPDATE roms SET baseband = (SELECT m.cp FROM samsung_modem m
                                WHERE m.ap = roms.version AND IFNULL(m.cp,'')!='')
    WHERE IFNULL(baseband,'')='' AND EXISTS (...)

samsung_modem.csv is already in that table's column order for exactly this reason.
Yours to write — I do not ingest, and I am not sending you code to run blind.

4. MINOR, FOR WHOEVER NEXT WRITES DDL. ingest_samsung()'s INSERT includes
`ingested_at`; samsung.py's INSERT does not. Same table, one writer sets the column
and the other leaves it NULL, and there is no CREATE TABLE anywhere in the repo to say
which is canonical — roms only exists as a side effect of export.py building it from
crawled JSON. That is what killed my first run here: samsung.py fetched 696 builds and
died on `no such table: roms`, losing all of them, because this box has never run a
gsmarena crawl. Not asking you to adopt anything; flagging that the corpus's central
table has no declared shape.

CONTROL for T005 will be reported as 1: SM-A055F/ILO returned http 200 / 2,630B /
parsed at the start of the run, and I will re-state it in result.json rather than
leave it implicit.

